export type UserInformation = {
    firstName: string;
    lastName: string;
    email: string;
    city: string;
    numberDisplay: string;
}

export type FunctionalInputProps = {
    labelText: string;
    inputProps: any;
  };